apiVersion: v1
kind: Service
metadata:
  name: issuance-service
spec:
  selector:
    app: issuance-service
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP # internal communication; use NodePort or LoadBalancer if external access needed
